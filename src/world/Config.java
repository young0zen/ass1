package world;

public class Config {
    Point center;
    double alpha;

    Config(double x, double y, double alpha) {
        center.x = x;
        center.y = y;
        this.alpha = alpha;
    }
}
